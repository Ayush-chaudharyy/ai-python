# DATATYPES (7-TYPES)
1. Numerical types :-.Intiger(int)
                   .floating(float)
                   .complex number(complex)


2. Sequence type :-.List(mutable)
                  .Tuple(immutable)
                  .Range(immutable)


3. Text Type :-.String(str)

4. Mapping Type:-.Dictionary(dict)

5. Bollen Type:-.Boolean(Bool)

6. Callable TYPE:-.Function
                  
7. User defined Type:-.classes
                      .object


